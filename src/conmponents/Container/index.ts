import { Container as DefaultContainer } from "@mui/material";
import styled from "styled-components";

export const Container = styled(DefaultContainer)`
position: relative;
padding: 0 1rem; 
margin-top: 1.5rem;
display: flex;
flex-direction: column;
gap: 2rem;
`